# Vite 开发服务器配置
# 这些变量只在开发环境生效，生产环境构建时不会使用

# 开发服务器主机地址（默认：127.0.0.1）
# 设置为 0.0.0.0 允许局域网访问
VITE_DEV_HOST="127.0.0.1"

# 开发服务器端口（默认：5173）
VITE_DEV_PORT="5173"

# API 代理目标地址（默认：http://localhost:3001）
# 如果后端服务运行在不同地址，请修改此配置
VITE_PROXY_TARGET="http://localhost:3001"
